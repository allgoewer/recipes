# Print recipes to a receipt printer

Call `bin/precipe` to select and print to lp -d bixolon.

Call `bin/to-md.py kässpatzn.yml | typst compile -f - eierlikör.png` to generate the following example:

![](example.png)
